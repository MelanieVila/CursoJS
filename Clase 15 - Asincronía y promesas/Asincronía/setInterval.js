// SET INTERVAL

// Actúa igual que setTimeout, sólo que se va a repetir ininterrumpidamente
//setTimeout = 1 vez;
//setInterval = non Stop;

//setInterval (código, tiempo-en-milisegundos):
setInterval(() => { console.log("Tic") }, 1000)